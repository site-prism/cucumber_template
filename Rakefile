# frozen_string_literal: true

# In here you would place your Rake tasks.
#
# Because we are not directly using rails, if you wanted to store them in `lib/` you would need to
# verbosely load that directory using a require_relative call
#
# Ideally this would be in the form of require_relative 'lib/all' and then in that file you'd control all the files
# you wanted to require
